# backend/persistence-service/requirements.txt
# Dépendances Python pour le microservice Persistence Service

# Framework web asynchrone
fastapi~=0.100.0

# Serveur ASGI pour FastAPI
uvicorn[standard]~=0.22.0

# Client HTTP asynchrone pour les appels inter-services (si le service de persistance appelle d'autres services, ce qui est moins courant)
httpx~=0.27.0

# Validation des données (dépendance de FastAPI)
pydantic~=2.0.0
pydantic-settings~=2.0.0 # Pour la gestion des paramètres de configuration

# ORM (Object-Relational Mapper) pour interagir avec la base de données
sqlalchemy~=2.0.0
# Driver asynchrone pour PostgreSQL
asyncpg~=0.29.0 # Recommandé pour SQLAlchemy 2.0+ avec PostgreSQL asynchrone
# Adaptateur PostgreSQL (psycopg2-binary est souvent utilisé avec asyncpg)
psycopg2-binary~=2.9.0

# Outil de migration de base de données
alembic~=1.13.0

# Dépendances partagées (ne sont pas installées directement via requirements.txt ici,
# mais sont copiées dans l'image Docker et importées comme modules locaux)
# shared (config, models, exceptions, utils)
