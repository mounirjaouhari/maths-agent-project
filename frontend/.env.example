# frontend/.env.example
# Exemple de fichier de variables d'environnement pour l'application frontend.
# Renommez ce fichier en .env pour l'utiliser en développement local.
# Ces variables sont chargées par les outils de build frontend (ex: create-react-app, Vite).

# URL de base de l'API Gateway du backend.
# Assurez-vous que cette URL est accessible depuis le navigateur où l'application frontend s'exécute.
# En développement local, cela pourrait être l'URL de votre API Gateway locale ou exposée.
# En production, cela serait l'URL publique de votre API Gateway.
REACT_APP_API_BASE_URL=http://localhost:8000/v1

# Vous pouvez ajouter d'autres variables d'environnement ici si votre frontend en a besoin.
# Par exemple, des clés pour des services tiers, des drapeaux de fonctionnalités, etc.
# REACT_APP_FEATURE_FLAG_X=true
# REACT_APP_ANALYTICS_ID=UA-XXXXXXXXX-Y

